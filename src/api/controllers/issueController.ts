import { Request, Response } from 'express';

export const createIssue = (req: Request, res: Response): void => {
  // Placeholder logic: just return 201 Created with an empty object for now
  res.status(201).json({});
};
