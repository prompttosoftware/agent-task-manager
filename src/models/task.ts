import { BaseIssue } from './baseIssue';

/**
 * Represents a standard task issue.
 * It extends the `BaseIssue` interface, inheriting common properties shared by all issue types.
 */
export interface Task extends BaseIssue {}
