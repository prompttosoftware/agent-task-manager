// test/setup.ts
import { vi } from 'vitest';

// Mock any dependencies here if needed.

// Example: Mock a database connection
// vi.mock('../src/config/database.config.ts', () => ({
//   default: { connection: { query: vi.fn() } },
// }));
